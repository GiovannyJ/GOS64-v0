Attach to docker:
-bash:
docker run --rm -it -v %cd%:/root/env myos-buildenv
-powershell:
docker run --rm -it -v ${PWD}:/root/env myos-buildenv
-mac/linux:
docker run --rm -it -v $(pwd):/root/env myos-buildenv

Start Up qemu for OS to run:
qemu-system-x86_64 -cdrom .\dist\x86_64\kernel.iso 